// Keep the descriptive route available for existing links while making the
// domain root render the dashboard directly, without an extra HTTP redirect.
export { default } from "./stake-earn-contribute/page";
